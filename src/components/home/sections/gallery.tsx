'use client'; import { getEventImageCount, getImagesByEventTitle, getUniqueEvents
} from '@/data/gallery';
import { AnimatePresence, motion } from 'framer-motion';
import Image from 'next/image';
import { useCallback, useEffect, useState } from 'react';
import { createPortal } from 'react-dom';
import { PiCaretLeft, PiCaretRight, PiImage, PiX } from 'react-icons/pi';
import { Badge, Button, Text, Title } from 'rizzui'; export default function Gallery() { const [isModalOpen, setIsModalOpen] = useState(false); const [selectedEventTitle, setSelectedEventTitle] = useState<string | null>(null); const [currentImageIndex, setCurrentImageIndex] = useState(0); const [mounted, setMounted] = useState(false); useEffect(() => { setMounted(true); }, []); const handleEventClick = (eventTitle: string) => { setSelectedEventTitle(eventTitle); setCurrentImageIndex(0); setIsModalOpen(true); }; const modalImages = selectedEventTitle ? getImagesByEventTitle(selectedEventTitle) : []; const goToPrevious = useCallback(() => { setCurrentImageIndex((prev) => (prev === 0 ? modalImages.length - 1 : prev - 1)); }, [modalImages.length]); const goToNext = useCallback(() => { setCurrentImageIndex((prev) => (prev === modalImages.length - 1 ? 0 : prev + 1)); }, [modalImages.length]); useEffect(() => { if (!isModalOpen) return; const handleKeyDown = (e: KeyboardEvent) => { if (e.key === 'Escape') setIsModalOpen(false); else if (e.key === 'ArrowLeft') goToPrevious(); else if (e.key === 'ArrowRight') goToNext(); }; window.addEventListener('keydown', handleKeyDown); return () => window.removeEventListener('keydown', handleKeyDown); }, [isModalOpen, modalImages.length, goToNext, goToPrevious]); useEffect(() => { document.body.style.overflow = isModalOpen ? 'hidden' : 'unset'; return () => { document.body.style.overflow = 'unset'; }; }, [isModalOpen]); const uniqueEvents = getUniqueEvents(); const modalContent = ( <AnimatePresence> {isModalOpen && modalImages.length > 0 && ( <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} className="fixed inset-0 z-[9999] bg-black/95 backdrop-blur-sm flex items-center justify-center p-4" onClick={() => setIsModalOpen(false)} > <button onClick={() => setIsModalOpen(false)} className="absolute top-4 right-4 z-50 h-12 w-12 bg-background/10 hover:bg-background/20 rounded-full flex items-center justify-center text-white transition-all backdrop-blur-md" > <PiX size={24} /> </button> {modalImages.length > 1 && ( <> <button onClick={(e) => { e.stopPropagation(); goToPrevious(); }} className="absolute left-4 z-50 h-14 w-14 bg-background/10 hover:bg-background/20 rounded-full flex items-center justify-center text-white transition-all backdrop-blur-md" > <PiCaretLeft size={28} /> </button> <button onClick={(e) => { e.stopPropagation(); goToNext(); }} className="absolute right-4 z-50 h-14 w-14 bg-background/10 hover:bg-background/20 rounded-full flex items-center justify-center text-white transition-all backdrop-blur-md" > <PiCaretRight size={28} /> </button> </> )} <motion.div initial={{ scale: 0.9, opacity: 0 }} animate={{ scale: 1, opacity: 1 }} exit={{ scale: 0.9, opacity: 0 }} className="relative max-w-6xl w-full max-h-[85vh] flex flex-col items-center" onClick={(e) => e.stopPropagation()} > <div className="relative w-full aspect-video bg-black/40 rounded-2xl overflow-hidden shadow-2xl border border-white/10"> <Image src={modalImages[currentImageIndex].src} alt={modalImages[currentImageIndex].alt} fill className="object-contain" sizes="90vw" priority /> </div> <div className="mt-8 text-center text-white"> <Title as="h3" className="text-2xl font-bold mb-2 text-white">{modalImages[currentImageIndex].title}</Title> <Text className="text-white/70 max-w-2xl mx-auto font-light">{modalImages[currentImageIndex].description}</Text> <div className="mt-4 flex items-center justify-center gap-4 text-sm text-white/50"> <Badge variant="flat" className="bg-primary/20 text-primary border-primary/20">{modalImages[currentImageIndex].category}</Badge> <span>{currentImageIndex + 1} / {modalImages.length}</span> </div> </div> </motion.div> </motion.div> )} </AnimatePresence> ); return ( <section id="gallery" className="relative py-24 lg:py-32 overflow-x-hidden"> <div className="container mx-auto px-4 sm:px-6 lg:px-8 max-w-[1440px]"> <div className="grid grid-cols-1 gap-12 lg:grid-cols-12 mb-20"> <motion.div initial={{ opacity: 0, x: -40 }} whileInView={{ opacity: 1, x: 0 }} viewport={{ once: true }} className="lg:col-span-2" > <span className="text-7xl font-bold text-gray-200 lg:text-9xl">04</span> <div className="mt-2 h-1.5 w-20 md:w-40 bg-gradient-to-r from-primary to-secondary rounded-full" /> </motion.div> <motion.div initial={{ opacity: 0, x: 40 }} whileInView={{ opacity: 1, x: 0 }} viewport={{ once: true }} className="lg:col-span-10" > <Title as="h2" className="mb-4 text-4xl font-bold text-foreground lg:text-7xl tracking-tight">Community Gallery</Title> <Text className="text-xl lg:text-2xl font-light leading-relaxed">Moments from our global events, workshops, and networking mixers where founders turn ideas into reality.</Text> </motion.div> </div> <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6"> {uniqueEvents.map((event, index) => ( <motion.div key={event.id} initial={{ opacity: 0, y: 30 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: index * 0.1 }} onClick={() => handleEventClick(event.title)} className="group relative aspect-[4/3] overflow-hidden rounded-3xl bg-gray-100 /50 cursor-pointer shadow-sm hover:shadow-2xl transition-all duration-500" > <Image src={event.src} alt={event.alt} fill className="object-cover transition-transform duration-700 group-hover:scale-110" sizes="(max-width: 768px) 50vw, 25vw" /> <div className="absolute inset-0 bg-gradient-to-t from-gray-900 via-gray-900/20 to-transparent opacity-60 group-hover:opacity-80 transition-opacity" /> <div className="absolute inset-0 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-all duration-500 scale-90 group-hover:scale-100"> <Button variant="flat" className="bg-transparent font-bold text-primary-foreground hover:bg-background backdrop-blur-[2px] rounded-xl px-6">View Event</Button> </div> <div className="absolute bottom-0 left-0 right-0 p-6 translate-y-2 group-hover:translate-y-0 transition-transform duration-500"> <div className="flex items-center gap-2 mb-2"> <Badge variant="flat" className="bg-primary/50 text-primary-foreground border-none text-[10px] tracking-wider uppercase">{event.category}</Badge> <span className="flex items-center gap-1.5 text-[10px] font-bold uppercase tracking-widest"> <PiImage size={12} className="text-primary" /> {getEventImageCount(event.title)} </span> </div> <Title as="h4" className="text-lg font-bold mb-1 text-white dark:text-background line-clamp-1">{event.title}</Title> <Text className="text-xs font-medium text-white dark:text-background">{event.date}</Text> </div> </motion.div> ))} </div> </div> {mounted && createPortal(modalContent, document.body)} </section> );
}
