'use client'; import { useState } from 'react';
import { PiImageBold, PiVideoCameraBold, PiCalendarBlankBold, PiArticleBold, PiXBold, PiGlobeSimpleBold, PiUsersBold, PiLockBold, PiCaretDownBold
} from 'react-icons/pi';
import { Title, Text, Button, Avatar, ActionIcon, Textarea } from 'rizzui';
import cn from '@/utils/class-names';
import { Modal } from '@/modal-views/modal';
import { useAuth } from '@/context/auth-context';
import { getApiMediaUrl } from '@/utils/get-api-media-url'; export default function CreatePost() { const { user } = useAuth(); const [isOpen, setIsOpen] = useState(false); const [content, setContent] = useState(''); const [visibility, setVisibility] = useState('public'); const handleOpen = () => setIsOpen(true); const handleClose = () => { setIsOpen(false); setContent(''); }; const actions = [ { label: 'Photo', icon: PiImageBold, color: 'text-blue-500', bg: 'bg-blue-50/50' }, { label: 'Video', icon: PiVideoCameraBold, color: 'text-green-500', bg: 'bg-green-50/50' }, { label: 'Event', icon: PiCalendarBlankBold, color: 'text-amber-500', bg: 'bg-amber-50/50' }, { label: 'Article', icon: PiArticleBold, color: 'text-rose-500', bg: 'bg-rose-50/50' }, ]; return ( <> <div className="rounded-2xl border border-muted bg-background p-4 shadow-sm"> <div className="flex gap-3 mb-4"> <Avatar name={user?.name || "User"} src={getApiMediaUrl(user?.avatarURL) || "/growthlab/founder.jpg"} size="md" className="ring-2 ring-primary/10 ring-offset-2 ring-offset-white dark:ring-offset-gray-900" /> <button onClick={handleOpen} className="flex-1 rounded-full bg-gray-50 px-6 text-left text-sm font-medium text-gray-500 hover:bg-gray-100 transition-colors border border-transparent hover:border-gray-200" > Start a post... </button> </div> <div className="flex justify-between gap-2 overflow-x-auto no-scrollbar pt-1"> {actions.map((action) => ( <Button key={action.label} variant="text" className={cn( "flex-1 h-10 gap-2 whitespace-nowrap rounded-xl text-xs font-bold transition-all", "hover:bg-gray-100 dark:hover:bg-gray-200 text-gray-600" )} onClick={handleOpen} > <action.icon className={cn("h-5 w-5", action.color)} /> {action.label} </Button> ))} </div> </div> {/* Post Modal */} <Modal isOpen={isOpen} onClose={handleClose} size="lg" rounded="lg" > <div className="p-6"> <div className="flex items-center justify-between mb-6"> <Title as="h3" className="text-xl font-bold">Create a post</Title> <ActionIcon variant="text" onClick={handleClose} className="rounded-full hover:bg-gray-100 border border-gray-200"> <PiXBold className="h-5 w-5" /> </ActionIcon> </div> <div className="flex gap-3 mb-6"> <Avatar name={user?.name || "User"} src={getApiMediaUrl(user?.avatarURL) || "/growthlab/founder.jpg"} size="md" /> <div> <Title as="h4" className="text-sm font-bold">{user?.name || "User"}</Title> <Button variant="outline" size="sm" className="h-7 px-2 mt-1 gap-1.5 rounded-lg border-gray-200 text-gray-500 font-bold text-[11px]" > {visibility === 'public' ? <PiGlobeSimpleBold className="h-3.5 w-3.5" /> : visibility === 'connections' ? <PiUsersBold className="h-3.5 w-3.5" /> : <PiLockBold className="h-3.5 w-3.5" />} <span className="capitalize">{visibility}</span> <PiCaretDownBold className="h-3 w-3" /> </Button> </div> </div> <Textarea placeholder="What do you want to talk about?" className="min-h-[220px] mb-6" textareaClassName="border-none focus:ring-0 text-lg p-0 resize-none no-scrollbar bg-transparent placeholder:text-gray-300" value={content} onChange={(e) => setContent(e.target.value)} /> <div className="flex flex-wrap gap-2 mb-6"> {actions.map((action) => ( <ActionIcon key={action.label} variant="flat" className={cn("h-10 w-10 rounded-xl", action.bg, action.color, "hover:opacity-80")} > <action.icon className="h-6 w-6" /> </ActionIcon> ))} </div> <div className="flex justify-between items-center pt-4 border-t border-muted"> <div className="text-xs text-gray-400 font-medium"> {content.length} / 3000 characters </div> <div className="flex gap-3"> <Button variant="outline" onClick={handleClose} className="rounded-xl font-bold border-gray-200"> Cancel </Button> <Button disabled={!content.trim()} className="rounded-xl font-extrabold px-8 bg-primary hover:bg-primary/90 text-white shadow-lg shadow-primary/20" > Post </Button> </div> </div> </div> </Modal> </> );
}
